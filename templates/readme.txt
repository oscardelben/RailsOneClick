This directory contains all the projects that you created with
RailsOneClick.

The ruby directory contains your entire ruby installation

These projects are intended to be opened with the RailsOneClick app.

If you want to run a rails app from the terminal, make sure you run
 this command before doing anything else:

export PATH=~/Documents/rails_one_click/ruby/bin:$PATH
